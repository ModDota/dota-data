{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "run-s build:packages build:ts",
    "build:packages": "lerna --no-private run --parallel build",
    "build:ts": "tsc -b packages",
    "dev": "run-p dev:packages dev:ts",
    "dev:packages": "lerna --no-private run --parallel dev",
    "dev:ts": "tsc -b packages --watch",
    "lint": "run-p lint:prettier lint:eslint",
    "lint:eslint": "eslint . --ext .js,.ts,.tsx",
    "lint:prettier": "prettier --check **/*.{ts,tsx,js,md,yml,json}",
    "prepublishOnly": "yarn build",
    "test": "jest",
    "web:build": "yarn workspace @dota-data/web build",
    "web:dev": "yarn workspace @dota-data/web dev"
  },
  "prettier": {
    "printWidth": 100,
    "proseWrap": "always",
    "singleQuote": true,
    "trailingComma": "all"
  },
  "eslintConfig": {
    "extends": "@ark120202/eslint-config/node",
    "parserOptions": {
      "project": [
        "tsconfig.json",
        "packages/*/tsconfig.json",
        "packages/dota-data/build/tsconfig.json"
      ]
    }
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.[jt]s?(x)",
      "!**/_*"
    ]
  },
  "devDependencies": {
    "@ark120202/eslint-config": "^2.2.0",
    "@ark120202/typescript-config": "^2.1.0",
    "@types/fs-extra": "^8.0.1",
    "@types/jest": "^24.0.23",
    "@types/lodash": "^4.14.149",
    "@types/prettier": "^1.19.0",
    "eslint": "^6.7.2",
    "eslint-plugin-react": "^7.17.0",
    "eslint-plugin-react-hooks": "^2.3.0",
    "jest": "^24.9.0",
    "lerna": "^3.19.0",
    "nodemon": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.19.1",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.3"
  }
}
